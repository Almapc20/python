class Solution:
    def merge(self, nums1, m, nums2, n) -> None:
        """
        Do not return anything, modify nums1 in-place instead.
        """
        global numList
        nums1=nums1[:m]
        numList= nums1+nums2
       
    def sortList(self, x):
        global list1
        list1 = []
        list1= sorted(x) 
        print(list1)

nums1 = [1,2,3,0,0,0]
m = 3
nums2 = [2,5,6]
n = 3

sol= Solution()
sol.merge(nums1, m, nums2, n)
sol.sortList(numList)
